<?xml version='1.0' encoding='utf-8'?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro" name="fr3_mujoco">

  <xacro:include filename="$(find franka_description)/robots/common/franka_robot.xacro"/>
  <xacro:include filename="$(find crisp_controllers_robot_demos)/config/fr3/fr3.ros2_control.urdf.xacro"/>

  <xacro:macro name="fr3"
         params="arm_id
             robot_ip
             use_fake_hardware
             arm_prefix:=''
             hand:=true
             ee_id:='franka_hand'
             with_sc:=false
             gazebo:=false
             gazebo_effort:=true
             fake_sensor_commands:=false
             no_prefix:=false
             "
    >
  <xacro:property name="arm_prefix_modified" value="${'' if arm_prefix == '' else arm_prefix + '_'}" />
  <!-- franka_description 2.8.1 franka_robot signature: arm_id -> robot_type,
       and the macro now loads joint_limits/inertials/kinematics/dynamics
       internally from robot_type. Hardware params (robot_ip, use_fake_hardware,
       ros2_control, ...) are no longer accepted here — our own
       franka_arm_ros2_control macro below provides the ros2_control block. -->
  <xacro:franka_robot robot_type="${arm_id}"
                      gazebo="${gazebo}"
                      hand="${hand}"
                      ee_id="${ee_id}"
                      with_sc="${with_sc}"
                      no_prefix="${no_prefix}"
                      arm_prefix="${arm_prefix}"
                      connected_to="${arm_prefix_modified}base">
  </xacro:franka_robot>

  <!-- We use our own ros2 control definition -->
  <xacro:franka_arm_ros2_control arm_id="fr3"
            robot_ip="${robot_ip}"
            use_fake_hardware="$(arg use_fake_hardware)"
            arm_prefix="${arm_prefix}"/>


  </xacro:macro>

</robot>
